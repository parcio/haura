[package]
name = "betree-tests"
version = "0.1.0"
authors = ["tilpner <betree@tilpner.com>"]
edition = "2018"

[dependencies]
betree_storage_stack = { path = "..", features = [ "internal-api" ] }
insta = "1.7"
serde_json = "1"
rstest = "0.13"

rand = "0.8"
rand_xoshiro = "0.6"
